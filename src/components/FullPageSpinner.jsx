import Spinner from "./Spinner";

function FullPageSpinner() {
  return (
    <div className="h-screen w-full bg-[var(--background-color)]">
      <Spinner />
    </div>
  );
}

export default FullPageSpinner;
